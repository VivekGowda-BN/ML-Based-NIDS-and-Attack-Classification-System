# =============================================================================
#  ML-Based NIDS — requirements.txt
#  Tested against Python 3.13.  Pin to stable releases with upper bounds so
#  that a fresh `pip install -r requirements.txt` is reproducible.
# =============================================================================

# ─── Core data / numerical computing ─────────────────────────────────────────
numpy>=1.26.0,<3.0           # 2.x series is supported by all deps below
pandas>=2.2.0,<3.0
pyarrow>=15.0.0,<21.0        # fast Parquet / Arrow backend for pandas

# ─── Machine learning ─────────────────────────────────────────────────────────
scikit-learn>=1.4.0,<2.0
imbalanced-learn>=0.13.0,<0.16.0  # SMOTE & over/under-sampling strategies
joblib>=1.3.0,<2.0              # model serialisation (already a sklearn dep)

# ─── Gradient boosting ────────────────────────────────────────────────────────
xgboost>=2.0.0,<3.0

# ─── Explainability ───────────────────────────────────────────────────────────
# Prebuilt Windows wheels (including abi3/cp313) are available for SHAP >= 0.48.0.
# If SHAP is unavailable or cannot be imported, the application falls back to permutation importance (see explain.py).
shap>=0.48.0,<1.0.0

# ─── Visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.8.0,<4.0
seaborn>=0.13.0,<0.14
plotly>=5.20.0,<7.0

# ─── Dashboard ────────────────────────────────────────────────────────────────
streamlit>=1.35.0,<2.0

# ─── Validation ───────────────────────────────────────────────────────────────
pydantic>=2.7.0,<3.0

# ─── Config / utilities ───────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0

# ─── Notebooks ────────────────────────────────────────────────────────────────
jupyter>=1.0.0,<2.0
ipykernel>=6.29.0,<7.0

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.2.0,<9.0
pytest-cov>=5.0.0,<7.0
